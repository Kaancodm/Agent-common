interface:
  display_name: "Agent Common"
  short_description: "Deliver general project tasks"
  default_prompt: "Work this task independently through to a usable and verified result."
policy:
  allow_implicit_invocation: true
